#include <stdio.h>
#include "share.h"

extern int colors[COLORS];

char *players_list(int, int*);
char *ranking_list(int, int*);
int d_list2_proc(int, DIALOG*, int);
int d_list3_proc(int, DIALOG*, int);
